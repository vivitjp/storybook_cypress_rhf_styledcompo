import React from "react"
import ReactDOM from "react-dom/client"
import { App_reactHookForm } from "./App_reactHookForm"
import "./index.css"

const root = ReactDOM.createRoot(document.getElementById("root") as HTMLElement)
root.render(
  <React.StrictMode>
    <App_reactHookForm />
  </React.StrictMode>
)
